<%- include('partials/header') %>

<div class="product-page">
  <a href="/" class="back-link">&larr; Back to Store</a>

  <div class="product-detail">
    <div class="product-detail-image">
      <img src="/images/<%= product.image %>" alt="<%= product.name %>">
    </div>
    <div class="product-detail-info">
      <h1 class="product-detail-name"><%= product.name %></h1>
      <span class="product-category-badge"><%= product.category %></span>
      <p class="product-detail-price">$<%= product.price.toFixed(2) %></p>
      <p class="product-detail-description"><%= product.description %></p>

      <form method="POST" action="/cart/add" class="product-actions">
        <input type="hidden" name="product_id" value="<%= product.id %>">
        <input type="hidden" name="from" value="/product/<%= product.id %>">
        <div class="quantity-selector">
          <label for="quantity">Qty:</label>
          <input type="number" id="quantity" name="quantity" value="1" min="1" max="10">
        </div>
        <button type="submit" class="btn btn-primary add-to-cart-btn" title="Add to Cart">Add to Cart</button>
      </form>
    </div>
  </div>
</div>

<%- include('partials/footer') %>
